import { StockClient } from "@/components/stock/StockClient";

export const metadata = {
  title: "Estoque | PantryPro",
  description: "Controle de estoque",
};

export default function StockPage() {
  return <StockClient />;
}
