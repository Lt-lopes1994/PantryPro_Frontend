import { PurchasesClient } from "@/components/purchases/PurchasesClient";

export const metadata = {
  title: "Compras | PantryPro",
  description: "Gestão de compras",
};

export default function PurchasesPage() {
  return <PurchasesClient />;
}
